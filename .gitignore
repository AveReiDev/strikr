# Dev TLS material — generated by serve.py, private key must never be committed
dev-cert.pem
dev-key.pem

# macOS / tooling
.DS_Store
node_modules/
.claude/settings.local.json

# Screenshots of a third-party app used as design reference — kept locally, not published
reference/
